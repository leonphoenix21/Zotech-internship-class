// First reason will be for first and lastname 

function firstnamelastname(fn, ln) {
    return fn + ' ' + ln
}

const firstname = 'Noel'
const lastname = 'Mutia'

console.log(firstnamelastname(firstname, lastname))